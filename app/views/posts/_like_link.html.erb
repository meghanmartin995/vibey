<%# vote_text = @voter&.voted_for?(post) ? "Unlike" : "Like" %>

<%= button_to like_post_path(post), method: :put, remote: :true, class: 'like-post' do %>
  <% if @voter&.voted_for?(post) %>
    <i class="fa fa-heart"></i>
  <% else %>
    <i class="far fa-heart"></i>
  <% end %>
<% end %>
